services:
  blazinglyfast:
    build: .
    ports:
      - "1337:1337"
    security_opt:
      - "seccomp=unconfined"
      - "apparmor=unconfined"
    cap_add:
      - SYS_ADMIN
    tmpfs:
      - /tmp:exec
